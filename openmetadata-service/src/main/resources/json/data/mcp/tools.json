{
  "tools": [
    {
      "name": "search_metadata",
      "description": "Search for metadata entities in OpenMetadata based on keywords or phrases",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "The search query or keywords to find relevant metadata"
          },
          "entity_type": {
            "type": "string",
            "description": "Optional entity type to filter results (e.g., 'table', 'dashboard', 'topic')"
          },
          "limit": {
            "type": "integer",
            "description": "Maximum number of results to return (default: 10)"
          }
        },
        "required": ["query"]
      }
    },
    {
      "name": "get_entity_details",
      "description": "Get detailed information about a specific entity when you know its fully qualified name (FQN)",
      "parameters": {
        "type": "object",
        "properties": {
          "entity_type": {
            "type": "string",
            "description": "The type of entity (e.g., 'table', 'dashboard', 'topic')"
          },
          "fqn": {
            "type": "string",
            "description": "The fully qualified name of the entity"
          }
        },
        "required": ["entity_type", "fqn"]
      }
    },
    {
      "name": "nlq_search",
      "description": "Search OpenMetadata using natural language queries",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Natural language query"
          },
          "entity_type": {
            "type": "string",
            "description": "Entity type to search in (default: table)"
          },
          "limit": {
            "type": "integer",
            "description": "Maximum number of results to return (default: 10)"
          }
        },
        "required": ["query"]
      }
    },
    {
      "name": "advanced_search",
      "description": "Perform advanced search with multiple filters and conditions",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Base search query"
          },
          "entity_type": {
            "type": "string",
            "description": "Entity type to search in"
          },
          "filters": {
            "type": "object",
            "description": "Additional filters to apply (key-value pairs)"
          },
          "sort_field": {
            "type": "string",
            "description": "Field to sort results by"
          },
          "sort_order": {
            "type": "string",
            "description": "Sort order (asc or desc)"
          },
          "limit": {
            "type": "integer",
            "description": "Maximum number of results to return (default: 10)"
          }
        },
        "required": ["query"]
      }
    }
  ]
}